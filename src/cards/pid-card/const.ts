import { PREFIX_NAME } from '../../const';

export const PID_CARD_NAME = `${PREFIX_NAME}-pid-card`;
export const PID_CARD_EDITOR_NAME = `${PID_CARD_NAME}-editor`;

export const SENSOR_ENTITY_DOMAINS: readonly string[] = ['sensor', 'input_number'];
export const CLIMATE_ENTITY_DOMAINS: readonly string[] = ['climate'];
